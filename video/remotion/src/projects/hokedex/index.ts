// Add more specs here as new videos are created for hokedex.
// Each spec exports a composition object registered in Root.tsx.
import { mainComposition } from './specs/main';

export const compositions = [mainComposition];
