// Register all core effects. Import this once at the entry point (spec.ts).
// Order does not matter — effects are stateless registry entries.
import './pop-in';
import './bloom';
import './tap-ring';
import './typewriter';
