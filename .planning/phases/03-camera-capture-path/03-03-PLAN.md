---
phase: 03-camera-capture-path
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/screens/CameraScreen.tsx
  - src/services/MomentCaptureService.ts
  - src/components/GalleryBottomSheet.tsx
  - src/components/GalleryPivot.tsx
  - android/app/src/main/java/com/hokedex/workers/FaceProcessingWorker.kt
  - android/app/src/main/java/com/hokedex/MainApplication.kt
autonomous: true
requirements: [R-CAM-01, R-CAM-02, R-CAM-05, R-CAM-09, R-CAM-10]
must_haves:
  truths:
    - "Capture saves a moment then enqueues a processing_queue job in the same flow — no FacePickerModal fires during capture"
    - "MomentCaptureService.capture accepts photoUri and enqueues to processing_queue inside the moment transaction"
    - "FaceProcessingWorker runs when idle + charging and drains the queue; rescheduled every 6h"
    - "GalleryBottomSheet and GalleryPivot are deleted and no longer referenced"
  artifacts:
    - path: "src/services/MomentCaptureService.ts"
      provides: "capture() that enqueues a queue job in-transaction"
    - path: "src/screens/CameraScreen.tsx"
      provides: "non-blocking capture with no face modals"
    - path: "android/app/src/main/java/com/hokedex/workers/FaceProcessingWorker.kt"
      provides: "CoroutineWorker draining the queue"
  key_links:
    - from: "src/screens/CameraScreen.tsx"
      to: "src/services/MomentCaptureService.ts"
      via: "captureService.capture with photoUri"
      pattern: "capture\\("
    - from: "android/app/src/main/java/com/hokedex/MainApplication.kt"
      to: "FaceProcessingWorker"
      via: "PeriodicWorkRequest registration"
      pattern: "FaceProcessingWorker|WorkManager"
---

<objective>
Refactor the capture path to be fully non-blocking: save the moment, enqueue a face-processing job, and remove all in-capture face modals. Delete dead gallery components. Add a WorkManager worker that drains the queue when the device is idle and charging.

Purpose: Capture must never block on face detection; annotation moves to the Moments page. Background processing handles embedding/matching durably.
Output: Slimmed CameraScreen, queue-enqueuing MomentCaptureService, deleted GalleryBottomSheet/GalleryPivot, FaceProcessingWorker registered in MainApplication.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@CLAUDE.md
@src/screens/CameraScreen.tsx
@src/services/MomentCaptureService.ts
@android/app/src/main/java/com/hokedex/MainApplication.kt
@src/db/queries/processing_queue.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: MomentCaptureService enqueues processing job in-transaction</name>
  <read_first>
    - src/services/MomentCaptureService.ts (capture() transaction, withTransaction, insertMoment)
    - src/db/queries/processing_queue.ts (from 03-01 — EnqueueJob)
    - src/db/tx.ts (withTransaction signature; whether tx supports the same execute API)
    - CLAUDE.md (R-CONV-04: MomentCaptureService is the only moment entry point; sync DDL vs async)
  </read_first>
  <action>
    Add optional `photoUri?: string | null` to CaptureInput. Inside capture()'s withTransaction block, after insertMoment and the people inserts, when photoUri is present, insert a processing_queue row for {momentId: id, photoUri} using the EnqueueJob query executed on the transaction handle (mirror how insertMoment/insertMomentPerson run on tx). Generate the queue id with the existing generateId() helper. Keep the existing return shape { momentId }. Do NOT call FaceProcessingQueue.drain here — capture only enqueues. Keep this synchronous within the transaction (same execute path the other inserts use).

    Leave addPeopleToMoment and getMomentWithPeople as-is.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/services/MomentCaptureService.ts','utf8');if(!s.includes('photoUri'))throw new Error('no photoUri');if(!/queue|Enqueue|processing_queue/i.test(s))throw new Error('no enqueue');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - CaptureInput has photoUri; capture() enqueues a queue row in the same transaction when photoUri present
    - No drain call inside capture
  </acceptance_criteria>
  <done>capture() atomically writes moment + queue job when photoUri is supplied.</done>
</task>

<task type="auto">
  <name>Task 2: Slim CameraScreen — remove face modals, delete dead gallery components</name>
  <read_first>
    - src/screens/CameraScreen.tsx (current handleCapture; all face/modal state at lines ~59-144, 283-357)
    - src/components/GalleryBottomSheet.tsx and src/components/GalleryPivot.tsx (confirm only CameraScreen references them)
    - src/components/FacePickerModal.tsx, src/components/PersonConfirmModal.tsx (imports to remove)
    - CLAUDE.md (no decision logic inline in components)
  </read_first>
  <action>
    Rewrite CameraScreen.tsx to remove the entire face-picker / person-confirm flow:
    - Delete state: photoUri, savedMomentId, metadata, faces, facePickerVisible, currentFaceIndex, confirmedEntryIds, newPeople, confirmModalVisible, confirmModalMode, currentMatch (keep lastPhotoUri, sheetOpen removal below, captureMode, isProcessing).
    - Delete handlers: addToConfirmed, addNewPerson, processNextFace, handleFaceSelected, handleConfirmMatch, handleReject, handleCreatePerson, handleSkipFace, handleDonePicking.
    - Delete imports for FacePickerModal, PersonConfirmModal, runDetect, runEmbedAndMatch, MatchResult, BoundingBox, GalleryBottomSheet.
    - Remove the FacePickerModal, PersonConfirmModal, and GalleryBottomSheet JSX and the sheetOpen state.
    - In handleCapture (camera + local + voice branches): after a successful captureService.capture(...), pass the new photoUri so the job is enqueued. For camera branch: capture photo → set lastPhotoUri → resolveCaptureMetadata → captureService.capture({..., source:'camera', photoUri: uri}) → toast "Moment saved" → setIsProcessing(false). For local branch: pass photoUri: uri, source:'gallery'. Voice branch unchanged except it has no photoUri. Remove the background runDetect().then(...) block entirely.
    - The gallery button keeps navigating to 'CollectionList'.

    Delete the files src/components/GalleryBottomSheet.tsx and src/components/GalleryPivot.tsx (use the shell rm; they are dead after this change). Grep the repo to confirm no other importer remains; if one exists, stop and report rather than break it.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/screens/CameraScreen.tsx','utf8');['FacePickerModal','PersonConfirmModal','GalleryBottomSheet','savedMomentId','handleDonePicking'].forEach(t=>{if(s.includes(t))throw new Error('still references '+t)});if(!s.includes('photoUri'))throw new Error('capture must pass photoUri');['src/components/GalleryBottomSheet.tsx','src/components/GalleryPivot.tsx'].forEach(f=>{if(fs.existsSync(f))throw new Error('not deleted '+f)});console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - No FacePickerModal/PersonConfirmModal/GalleryBottomSheet references in CameraScreen
    - capture() called with photoUri in camera and local branches
    - GalleryBottomSheet.tsx and GalleryPivot.tsx deleted; no remaining importers
  </acceptance_criteria>
  <done>CameraScreen captures non-blocking; dead gallery components removed.</done>
</task>

<task type="auto">
  <name>Task 3: FaceProcessingWorker (CoroutineWorker) + MainApplication registration</name>
  <read_first>
    - android/app/src/main/java/com/hokedex/MainApplication.kt (package registration pattern, onCreate)
    - android/app/src/main/java/com/hokedex/ml/HokedexMLModule.kt (existing HokedexML bridge entry points to reuse for detection)
    - android/app/build.gradle (confirm androidx.work dependency; add if missing)
    - CLAUDE.md (Kotlin: one class per file; no business logic in MainActivity; native modules only wire)
  </read_first>
  <action>
    Confirm androidx.work:work-runtime-ktx is a dependency in android/app/build.gradle; add it if absent.

    Create android/app/src/main/java/com/hokedex/workers/FaceProcessingWorker.kt — a single CoroutineWorker class. In doWork(), invoke the queue drain through the existing HokedexML pipeline path (the worker triggers native detection/embedding for pending jobs; it must NOT contain DB or ML business logic itself — delegate to the existing ML pipeline classes, one class per file). Return Result.success() on completion, Result.retry() on transient failure.

    In MainApplication.onCreate(), after loadReactNative(this), enqueue a PeriodicWorkRequest for FaceProcessingWorker with a 6-hour interval and Constraints requiresCharging(true) + requiresDeviceIdle(true), using enqueueUniquePeriodicWork with KEEP policy so it is registered once.

    Keep MainApplication free of business logic — only the WorkManager scheduling call.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const w=fs.readFileSync('android/app/src/main/java/com/hokedex/workers/FaceProcessingWorker.kt','utf8');if(!/CoroutineWorker/.test(w))throw new Error('not a CoroutineWorker');const m=fs.readFileSync('android/app/src/main/java/com/hokedex/MainApplication.kt','utf8');if(!/FaceProcessingWorker/.test(m))throw new Error('not registered');if(!/requiresCharging|requiresDeviceIdle|Constraints/.test(m))throw new Error('no constraints');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - FaceProcessingWorker.kt is a single CoroutineWorker, no inline DB/ML logic
    - MainApplication enqueues a unique periodic 6h work request with charging + idle constraints
    - androidx.work dependency present in build.gradle
  </acceptance_criteria>
  <done>Worker drains the queue on idle+charging and is registered once at app start.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| camera → filesystem | captured photo file path passed to queue/native ML |
| WorkManager → native ML | background job triggers on-device detection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | photo_uri in processing_queue | mitigate | store app-internal file:// URIs only; worker validates file exists before processing |
| T-03-02 | Denial of Service | drain loop | mitigate | bounded limit per drain; MarkJobFailed increments attempts to avoid infinite retry |
| T-03-03 | Information Disclosure | background ML | accept | all ML on-device (R-NF-02); no network egress of image data |
</threat_model>

<verification>
- `npx tsc --noEmit` introduces no new errors
- CameraScreen contains no face-modal references; gallery components deleted
- Worker compiles and is registered in MainApplication
</verification>

<success_criteria>
Capture saves a moment and enqueues a job without any face UI; the WorkManager worker drains pending jobs on idle+charging; dead gallery components are gone.
</success_criteria>

<output>
Create `.planning/phases/03-camera-capture-path/03-03-SUMMARY.md` when done.
</output>
