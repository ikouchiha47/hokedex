---
phase: 03-camera-capture-path
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/services/cameraCaptureFlow.ts
  - src/components/PersonConfirmModal.tsx
  - src/services/MomentCaptureService.ts
  - src/screens/CameraScreen.tsx
autonomous: false
requirements: [R-CAM-03, R-CAM-04, R-CAM-05, R-CAM-06, R-CAM-07, R-CAM-08, R-CAM-09, R-CAM-10]

must_haves:
  truths:
    - "Captured photo runs face detection and returns all detected faces with bounding boxes"
    - "Faces detected → user confirms via FacePickerModal; no face → moment created with source=camera, zero people, user notified"
    - "Confirmed face is embedded and matched; match above threshold prompts 'Is this [name]?'; no match prompts create-or-skip"
    - "MomentCaptureService.capture() atomically writes one moment (with lat/lon/place/weather/source) plus one moment_people row per confirmed person"
  artifacts:
    - path: "src/services/cameraCaptureFlow.ts"
      provides: "Pure runDetect + runEmbedAndMatch flow functions (no RN/DB-write imports)"
      contains: "searchEmbeddingsByVector"
    - path: "src/components/PersonConfirmModal.tsx"
      provides: "confirm_match and create_or_skip dialog"
      contains: "create_or_skip"
    - path: "src/services/MomentCaptureService.ts"
      provides: "Real atomic capture() implementation"
      contains: "withTransaction"
  key_links:
    - from: "src/screens/CameraScreen.tsx"
      to: "MomentCaptureService.capture"
      via: "handleCapture flow after metadata + detection + confirm"
      pattern: "\\.capture\\("
    - from: "src/services/MomentCaptureService.ts"
      to: "insertMoment + insertMomentPerson"
      via: "withTransaction"
      pattern: "insertMomentPerson"
---

<objective>
Complete the camera capture pipeline: face detection on the captured photo, face picker confirmation, embedding + match, person confirm/create dialog, and the real atomic `MomentCaptureService.capture()` write that persists a moment with all metadata plus one moment_people row per confirmed person.

This satisfies R-CAM-03 (detect → bounding boxes), R-CAM-04 (face picker), R-CAM-05 (no-face moment + notify), R-CAM-06 (embed + match), R-CAM-07 (match → confirm), R-CAM-08 (no match → create/skip), R-CAM-09 (atomic write with metadata), and completes R-CAM-10's metadata flow for voice mode.

Purpose: This is the integration wave. It consumes the viewfinder + capture trigger from 03-01 and the metadata resolver + schema from 03-02, and wires them into the full state machine inside CameraScreen, ending in one atomic DB write.
Output: cameraCaptureFlow.ts, PersonConfirmModal.tsx, real capture(), and the wired CameraScreen flow. Ends with an on-device E2E human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/CONVENTIONS.md
@.planning/phases/03-camera-capture-path/03-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cameraCaptureFlow.ts — pure detect + embed/match functions</name>
  <read_first>
    - src/types/ml.ts (DetectionResult union, BoundingBox)
    - src/services/ingestion.ts (embedCrop line 162, float32ToBuffer line 57 — reuse both)
    - src/db/queries/embeddings.ts (searchEmbeddingsByVector line 52, VectorSearchRow)
    - src/db/queries/entries.ts (getEntry line 27 — sync read for name display)
    - .planning/phases/03-camera-capture-path/03-RESEARCH.md (Pattern 1, Pattern 3, threshold 0.75)
  </read_first>
  <behavior>
    - runDetect(modules, imageUri, categoryId='people') → returns { faces: BoundingBox[] }: NO_SUBJECT → [], SUCCESS/LOW_CONFIDENCE → [crop], MULTI_SUBJECT → crops.
    - runEmbedAndMatch(modules, db, imageUri, crop, categoryId) for a match above 0.75 → { stage: 'match', entryId, name, score }.
    - runEmbedAndMatch for best_score below 0.75 (or no rows) → { stage: 'no_match' }.
  </behavior>
  <action>
    Create src/services/cameraCaptureFlow.ts with no React Native and no DB-write imports. Export `runDetect(modules: { HokedexML }, imageUri: string, categoryId: string): Promise<BoundingBox[]>` that calls HokedexML.detect and flattens the DetectionResult union to a BoundingBox[] (NO_SUBJECT → empty, SUCCESS/LOW_CONFIDENCE → single, MULTI_SUBJECT → array). Export `type MatchResult = { stage: 'match'; entryId: string; name: string; score: number } | { stage: 'no_match' }` and `async function runEmbedAndMatch(modules, db, imageUri, crop, categoryId): Promise<MatchResult>`: call embedCrop (from ingestion.ts) to get the number[] vector, convert with float32ToBuffer (from ingestion.ts), call searchEmbeddingsByVector, take matches[0]; if best_score >= SIMILARITY_THRESHOLD (const 0.75) call getEntry(db, entry_id) for the name and return stage:'match'; else return stage:'no_match'. Define SIMILARITY_THRESHOLD = 0.75 as a named const at top (no magic number). Do NOT write to the DB here and do NOT do async work that belongs in a transaction.
  </action>
  <verify>
    <automated>cd /Users/alexday/dev/hokedex && grep -q "runDetect" src/services/cameraCaptureFlow.ts && grep -q "runEmbedAndMatch" src/services/cameraCaptureFlow.ts && grep -q "0.75" src/services/cameraCaptureFlow.ts && ! grep -qE "react-native|insertMoment" src/services/cameraCaptureFlow.ts && npx tsc --noEmit 2>&1 | grep -v node_modules | grep -c "cameraCaptureFlow" | grep -q "^0$" && echo OK</automated>
  </verify>
  <done>runDetect flattens all 4 DetectionResult variants; runEmbedAndMatch returns match (with name) or no_match using the 0.75 threshold; no RN or DB-write imports.</done>
  <acceptance_criteria>
    - cameraCaptureFlow.ts exports runDetect and runEmbedAndMatch.
    - SIMILARITY_THRESHOLD named constant equals 0.75.
    - embedCrop and float32ToBuffer imported from ingestion.ts (not reimplemented).
    - No `react-native` import and no insertMoment/insertMomentPerson call in this file.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: PersonConfirmModal.tsx + real MomentCaptureService.capture()</name>
  <read_first>
    - src/components/FacePickerModal.tsx (modal styling conventions, lucide icon usage)
    - src/components/icons/index.ts (icon barrel — use existing exports)
    - src/services/MomentCaptureService.ts (CaptureInput extended in 03-02; capture() still a stub)
    - src/db/tx.ts (withTransaction — callback is synchronous)
    - src/db/queries/moments.ts (insertMoment params object from 03-02)
    - src/db/queries/moment_people.ts (insertMomentPerson line 32)
    - src/db/queries/entries.ts (insertEntry line 39 — for inline new-person create)
    - src/types/moments.ts (Result type)
  </read_first>
  <action>
    Create src/components/PersonConfirmModal.tsx — a controlled modal with two modes via a `mode: 'confirm_match' | 'create_or_skip'` prop. confirm_match shows "Is this {name}?" with Confirm / Not them actions (R-CAM-06/07). create_or_skip shows a name TextInput with Create person / Skip actions (R-CAM-08). Props: `visible`, `mode`, `name?`, `onConfirm(entryId | newName)`, `onReject`, `onSkip`, `onDismiss`. Use lucide-react-native icons from the icons barrel — no emoji. No business logic inside; it only renders and calls back.

    Implement the real capture() in MomentCaptureService.ts. Signature stays `async capture(input: CaptureInput): Promise<Result<CaptureResult>>`. Do all async work BEFORE the transaction (RESEARCH pitfall 2 — withTransaction callback is synchronous). Inside withTransaction(this.db, tx => {...}): call insertMoment(tx, { note, occurredAt, placeId, source, latitude, longitude, placeName, weatherTemp, weatherCondition, type }) to get momentId; for any new-person entries the caller passed (inline-created names), call insertEntry(tx, ...) first to mint entry rows; then for each confirmed entryId call insertMomentPerson(tx, momentId, entryId). Wrap in try/catch and return `{ ok: false, error }` on failure, `{ ok: true, value: { momentId } }` on success. This is the single write path (R-CONV-04) — no other module writes moments/moment_people. Update CaptureInput if needed so confirmed people and new-person names are expressible (e.g. keep `entryIds: string[]` for matched/existing, add `newPeople?: { name: string }[]` if inline create is supported — create their entries inside the same transaction so the write stays atomic, R-CAM-09).
  </action>
  <verify>
    <automated>cd /Users/alexday/dev/hokedex && grep -q "create_or_skip" src/components/PersonConfirmModal.tsx && grep -q "confirm_match" src/components/PersonConfirmModal.tsx && grep -q "withTransaction" src/services/MomentCaptureService.ts && grep -q "insertMomentPerson" src/services/MomentCaptureService.ts && ! grep -q "Not implemented" src/services/MomentCaptureService.ts && npx tsc --noEmit 2>&1 | grep -v node_modules | grep -cE "PersonConfirmModal|MomentCaptureService" | grep -q "^0$" && echo OK</automated>
  </verify>
  <done>PersonConfirmModal renders both modes; capture() does a single withTransaction writing moment + moment_people (+ any inline new entries) atomically; no "Not implemented" left.</done>
  <acceptance_criteria>
    - PersonConfirmModal supports confirm_match and create_or_skip modes, uses lucide icons (no emoji).
    - capture() uses withTransaction and is the only writer of moments/moment_people.
    - All async work (none needed in write — inputs are pre-resolved) precedes the synchronous transaction callback.
    - capture() returns Result with momentId on success and ok:false on error.
    - No "Not implemented" string remains in MomentCaptureService.ts.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire full capture state machine in CameraScreen</name>
  <read_first>
    - src/screens/CameraScreen.tsx (03-01 — handleCapture currently stores photo URI + mode state)
    - src/services/captureMetadataService.ts (resolveCaptureMetadata — from 03-02)
    - src/services/cameraCaptureFlow.ts (runDetect, runEmbedAndMatch — Task 1)
    - src/components/FacePickerModal.tsx (props: visible, imageUri, crops, onSelect, onAdd, onDismiss, loading)
    - src/components/PersonConfirmModal.tsx (Task 2)
    - src/services/MomentCaptureService.ts (capture — Task 2)
    - android/.../media/HokedexMediaModule.kt (HokedexMedia for reference) — but capture uses vision-camera takePhoto from 03-01
  </read_first>
  <action>
    In CameraScreen.tsx, expand handleCapture into the full state machine but keep zero business logic inline — orchestrate service calls only. On capture: (1) take photo via the vision-camera ref (from 03-01) to get imageUri; (2) start resolveCaptureMetadata() in parallel (R-CAM-02); (3) call runDetect(modules, imageUri, 'people'). If faces.length === 0 → await metadata, call MomentCaptureService.capture({ note:null, occurredAt: Date.now(), entryIds: [], source: 'camera', ...metadata }) and show a ToastAndroid "Moment saved (no people)" (R-CAM-05). If faces.length >= 1 → show FacePickerModal; for each selected crop run runEmbedAndMatch; if stage 'match' show PersonConfirmModal mode='confirm_match' with the name (R-CAM-06), on confirm accumulate entryId, on reject fall to create_or_skip; if stage 'no_match' show PersonConfirmModal mode='create_or_skip' (R-CAM-07/08) — Create accumulates a newPeople name, Skip drops the face. Loop faces one at a time (RESEARCH open question 4). When all faces resolved, await metadata and call MomentCaptureService.capture() ONCE with accumulated entryIds + newPeople + metadata + source='camera' (R-CAM-09). Hold state machine via React state (photoUri, faces, currentFaceIndex, confirmModal state, confirmedEntryIds, newPeople) — no ML or DB calls outside the service functions. Voice mode (captureMode==='voice') is out of scope for the full STT path (Phase 5) but the metadata flow is shared; leave a clear seam.
  </action>
  <verify>
    <automated>cd /Users/alexday/dev/hokedex && grep -q "resolveCaptureMetadata" src/screens/CameraScreen.tsx && grep -q "runDetect" src/screens/CameraScreen.tsx && grep -q "\.capture(" src/screens/CameraScreen.tsx && grep -q "ToastAndroid" src/screens/CameraScreen.tsx && ! grep -qE "HokedexML\.|insertMoment|searchEmbeddingsByVector" src/screens/CameraScreen.tsx && npx tsc --noEmit 2>&1 | grep -v node_modules | grep -c "CameraScreen" | grep -q "^0$" && echo OK</automated>
  </verify>
  <done>CameraScreen orchestrates capture → metadata(parallel) → detect → picker → embed/match → confirm/create → single capture() call; no ML or DB calls inline in the screen; tsc passes.</done>
  <acceptance_criteria>
    - CameraScreen calls resolveCaptureMetadata, runDetect, runEmbedAndMatch, and MomentCaptureService.capture.
    - No-face path calls capture() with empty entryIds and shows a Toast (R-CAM-05).
    - Faces resolved one at a time; capture() called exactly once at the end (R-CAM-09).
    - No direct HokedexML, searchEmbeddingsByVector, or insertMoment call in CameraScreen.tsx (facade respected).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: E2E-01 through E2E-05 on device</name>
  <what-built>
    Full camera capture pipeline: live viewfinder → capture → GPS+weather auto-attach → face detection → face picker → embed+match → person confirm/create → atomic moment write.
  </what-built>
  <how-to-verify>
    Build and install per CLAUDE.md: `cd android && ./gradlew assembleRelease --no-build-cache && adb install -r app/build/outputs/apk/release/app-release.apk && adb shell "am force-stop com.hokedex" && adb shell "am start -n com.hokedex/.MainActivity"`.
    1. E2E-01 (single new face): capture a photo of one new person, create them → moment saved, person linked.
    2. E2E-02 (single existing match): capture the same person again → "Is this [name]?" appears, confirm → moment linked to existing entry.
    3. E2E-03 (multiple faces): capture a group → confirm/create each → moment_people has one row per confirmed person.
    4. E2E-05 (no face): capture a scene with no people → toast shown, moment saved with zero people.
    5. Verify DB: `adb shell "am force-stop com.hokedex"; adb pull /sdcard/Android/data/com.hokedex/files/hokedex.db /tmp/hokedex.db; sqlite3 /tmp/hokedex.db "SELECT id, source, latitude, longitude, place_name, weather_temp, weather_condition FROM moments ORDER BY created_at DESC LIMIT 5;"` — rows show source='camera' and populated lat/lon + weather (where network/GPS available).
  </how-to-verify>
  <resume-signal>Type "approved" if E2E-01/02/03/05 pass and the DB shows metadata, or describe failures.</resume-signal>
  <acceptance_criteria>
    - E2E-01, E2E-02, E2E-03, E2E-05 pass on device.
    - moments rows show source='camera' with lat/lon/weather populated when signals available.
    - moment_people row count matches confirmed people per moment.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — on-device human verification; DB assertion via sqlite3 in steps above</automated>
  </verify>
  <done>Phase 3 E2E scenarios pass on device and DB metadata confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| captured image → ML | On-device face detect/embed; no image leaves device |
| UI → moments DB | All writes funnel through MomentCaptureService (R-CONV-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | partial moment write | mitigate | single withTransaction wraps moment + moment_people + inline entries (R-CAM-09 atomic) |
| T-03-07 | Information disclosure | face embeddings | mitigate | embedding + match run on-device via HokedexML; no network (R-NF-02) |
| T-03-08 | Repudiation | wrong person linked | accept | user explicitly confirms each match via PersonConfirmModal before write |
</threat_model>

<verification>
- All four E2E scenarios pass on device.
- moments rows carry source + lat/lon + weather; moment_people rows match confirmed people.
- No facade violation: ML/DB-write calls only inside services, never in CameraScreen.
</verification>

<success_criteria>
- R-CAM-03..R-CAM-09 satisfied end-to-end on device.
- R-CAM-09: capture() writes atomically with full metadata.
- R-CAM-10: metadata attach flow shared and ready for voice mode (full STT in Phase 5).
</success_criteria>

<output>
Create `.planning/phases/03-camera-capture-path/03-03-SUMMARY.md` when done.
</output>
