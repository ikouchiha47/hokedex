---
phase: 03-camera-capture-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - android/app/src/main/AndroidManifest.xml
  - src/screens/CameraScreen.tsx
  - src/utils/permissions.ts
autonomous: false
requirements: [R-CAM-01, R-CAM-02, R-CAM-03, R-CAM-10]
user_setup: []

must_haves:
  truths:
    - "Camera tab shows a live in-app viewfinder on launch after camera permission is granted"
    - "CameraBottomBar renders over the viewfinder with capture, gallery, and face-scan controls"
    - "Tapping the capture button takes a photo via vision-camera and yields a file path"
    - "Mode switcher (VIDEO · VOICE · CONTACT) state is held in CameraScreen and the active mode is visually indicated"
  artifacts:
    - path: "src/screens/CameraScreen.tsx"
      provides: "Live viewfinder + bottom bar + capture handler + mode state"
      contains: "react-native-vision-camera"
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "CAMERA and RECORD_AUDIO permissions for vision-camera"
      contains: "RECORD_AUDIO"
  key_links:
    - from: "src/screens/CameraScreen.tsx"
      to: "react-native-vision-camera Camera component"
      via: "camera ref takePhoto()"
      pattern: "takePhoto"
---

<objective>
Install and integrate react-native-vision-camera to deliver a live in-app viewfinder inside `CameraScreen.tsx` (the default Camera tab). Wire `CameraBottomBar.onCapturePress` to a real photo capture that returns a file path, and hold mode-switcher state (VIDEO · VOICE · CONTACT) in the screen.

This satisfies R-CAM-01 (viewfinder opens on launch), the photo-acquisition half of R-CAM-02, the photo-capture trigger of R-CAM-03 (detection wiring happens in 03-03), and the voice-mode entry point of R-CAM-10.

Purpose: Phase 2 delivers `CameraScreen` + `CameraBottomBar` as a stub viewfinder with `onCapturePress={() => {}}`. This plan replaces that stub region with a real camera preview and a working capture trigger, so 03-03 can wire detection onto the captured photo path.
Output: A live viewfinder, a capture handler that produces a photo path, and persisted mode-switcher state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/CONVENTIONS.md
@.planning/ROADMAP.md
@.planning/phases/03-camera-capture-path/03-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify react-native-vision-camera package legitimacy before install</name>
  <read_first>
    - .planning/phases/03-camera-capture-path/03-RESEARCH.md (Area 1: Live Camera Viewfinder — marked [ASSUMED])
  </read_first>
  <what-built>
    No code is built in this task. RESEARCH.md flags `react-native-vision-camera` as [ASSUMED] — not yet verified in this codebase. Per the package legitimacy gate, this checkpoint must clear before the install task runs.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/react-native-vision-camera
    2. Confirm it is the maintained community camera library (org: mrousavy), version 4.x, recent publish date, high weekly downloads.
    3. Confirm RN 0.86 + New Architecture compatibility is acceptable.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with install, or name an alternative library.</resume-signal>
  <acceptance_criteria>
    - User has explicitly approved react-native-vision-camera before any npm install runs.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — human checkpoint, no automated gate</automated>
  </verify>
  <done>User approved the package or selected an alternative.</done>
</task>

<task type="auto">
  <name>Task 2: Install vision-camera, add RECORD_AUDIO permission, rebuild native</name>
  <read_first>
    - android/app/src/main/AndroidManifest.xml (CAMERA already present line 6; RECORD_AUDIO missing)
    - package.json (confirm vision-camera absent)
    - CLAUDE.md (release build process — assembleRelease --no-build-cache)
  </read_first>
  <action>
    Run `npm install react-native-vision-camera` (per R-CAM-01, viewfinder requires a live preview component; system-intent capture does not satisfy "open viewfinder immediately"). Add `<uses-permission android:name="android.permission.RECORD_AUDIO" />` to AndroidManifest.xml — CAMERA is already present (line 6), do not duplicate it. vision-camera requires RECORD_AUDIO declared even for photo-only use. Do NOT add Kotlin module code — vision-camera autolinks under New Architecture. Rebuild the Android app with `cd android && ./gradlew assembleRelease --no-build-cache` so the native module links. Do not edit MainApplication.kt unless autolinking fails.
  </action>
  <verify>
    <automated>grep -q "react-native-vision-camera" /Users/alexday/dev/hokedex/package.json && grep -q "RECORD_AUDIO" /Users/alexday/dev/hokedex/android/app/src/main/AndroidManifest.xml && echo OK</automated>
  </verify>
  <done>vision-camera in package.json dependencies, RECORD_AUDIO in manifest, release APK builds without native link errors.</done>
  <acceptance_criteria>
    - `react-native-vision-camera` present in package.json dependencies.
    - AndroidManifest.xml contains exactly one CAMERA and one RECORD_AUDIO uses-permission line.
    - `./gradlew assembleRelease --no-build-cache` completes without unresolved-native-module errors.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Render live viewfinder in CameraScreen and wire capture + mode state</name>
  <read_first>
    - src/screens/CameraScreen.tsx (Phase 2 stub — the viewfinder region and onCapturePress={() => {}})
    - src/components/CameraBottomBar.tsx (Phase 2 — props: onCapturePress, onGalleryPress, onFaceScanPress, bottomInset)
    - src/utils/permissions.ts (requestCameraPermission already exists, line 3)
    - src/services/weather.ts (PermissionsAndroid.request pattern for reference)
  </read_first>
  <action>
    In CameraScreen.tsx, render vision-camera's `Camera` component as the base layer via `StyleSheet.absoluteFill`, using `useCameraDevice('back')` and `useCameraPermission()` from react-native-vision-camera. Request camera permission on mount (reuse requestCameraPermission from src/utils/permissions.ts; fall back to vision-camera's requestPermission if needed) before setting the camera active — per R-CAM-01 permission precedes preview. Keep `CameraBottomBar` rendered AFTER the Camera in JSX so it sits above in z-order (transparent background, absolute bottom). Hold a `cameraRef` (useRef) on the Camera. Implement `handleCapture` async: call `cameraRef.current.takePhoto()`, normalize the returned path to a `file://` URI, and store it in screen state `capturedPhotoUri` (03-03 consumes this — for now just set state and log). Wire `onCapturePress={handleCapture}`. Add `captureMode` state of type `'video' | 'voice' | 'contact'` (default 'video'), and a `setCaptureMode` handler wired to the VIDEO · VOICE · CONTACT labels so the active label is visually indicated. Do NOT add face detection, GPS, weather, or DB writes here — those are 03-02 and 03-03. No business logic beyond permission + capture + mode state; keep it a thin screen.
  </action>
  <verify>
    <automated>cd /Users/alexday/dev/hokedex && grep -q "useCameraDevice" src/screens/CameraScreen.tsx && grep -q "takePhoto" src/screens/CameraScreen.tsx && grep -q "captureMode" src/screens/CameraScreen.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "node_modules" | grep -c "CameraScreen" | grep -q "^0$" && echo OK</automated>
  </verify>
  <done>CameraScreen renders a live Camera behind CameraBottomBar, handleCapture calls takePhoto() and stores a photo URI, captureMode state drives the mode labels, tsc passes for the file.</done>
  <acceptance_criteria>
    - CameraScreen.tsx imports `Camera`, `useCameraDevice`, `useCameraPermission` from react-native-vision-camera.
    - `cameraRef.current.takePhoto()` is invoked inside the onCapturePress handler.
    - `captureMode` state exists with values video | voice | contact and is bound to the mode labels.
    - No call to `HokedexML`, `insertMoment`, or `HokedexGeocoder` appears in CameraScreen.tsx (those belong to 03-02/03-03).
    - `npx tsc --noEmit` reports no errors originating in CameraScreen.tsx.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → device camera | Camera/audio runtime permission gates hardware access |
| npm registry → build | New native dependency introduced into the app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information disclosure | live camera preview | mitigate | Camera set active only after permission granted; no frame data leaves device (R-NF-01) |
| T-03-02 | Elevation of privilege | RECORD_AUDIO permission | accept | Declared for vision-camera requirement; photo flow does not record audio |
| T-03-SC | Tampering | npm install react-native-vision-camera | mitigate | blocking-human legitimacy checkpoint (Task 1) before install |
</threat_model>

<verification>
- Live viewfinder renders on device launch after granting camera permission (E2E precondition).
- Capture button produces a photo file path (logged) — full pipeline wired in 03-03.
- Mode labels reflect active captureMode.
</verification>

<success_criteria>
- R-CAM-01: Camera tab opens a live in-app viewfinder after permission.
- R-CAM-02 (partial): capture takes a photo and yields a path for downstream metadata/detection.
- R-CAM-03 (partial): capture trigger exists; detection wired in 03-03.
- R-CAM-10 (partial): voice mode selectable from the mode switcher.
</success_criteria>

<output>
Create `.planning/phases/03-camera-capture-path/03-01-SUMMARY.md` when done.
</output>
