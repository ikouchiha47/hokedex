---
phase: 03-camera-capture-path
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/services/FaceProcessingQueue.ts
  - src/services/GapClusterService.ts
  - src/services/RegroupService.ts
  - src/services/__tests__/GapClusterService.test.ts
  - src/services/__tests__/RegroupService.test.ts
autonomous: true
requirements: [R-CAM-03, R-CAM-06, R-CAM-07, R-CAM-08]
must_haves:
  truths:
    - "FaceProcessingQueue.enqueue inserts a pending job; drain processes pending jobs into moment_faces and is idempotent"
    - "GapClusterService.cluster splits moments into sessions wherever the time gap exceeds 1 hour"
    - "RegroupService.regroup merges (never wipes) groups and serialises concurrent calls via idle/running/queued state machine"
  artifacts:
    - path: "src/services/FaceProcessingQueue.ts"
      provides: "enqueue + drain queue processing"
      exports: ["enqueue", "drain"]
    - path: "src/services/GapClusterService.ts"
      provides: "pure gap-based clustering"
      exports: ["cluster", "GAP_THRESHOLD_MS"]
    - path: "src/services/RegroupService.ts"
      provides: "regroup orchestration with concurrency state machine"
      exports: ["regroup"]
  key_links:
    - from: "src/services/FaceProcessingQueue.ts"
      to: "src/db/queries/processing_queue.ts"
      via: "claim + mark queries"
      pattern: "processing_queue"
    - from: "src/services/RegroupService.ts"
      to: "src/services/GapClusterService.ts"
      via: "cluster() call"
      pattern: "GapClusterService|cluster"
---

<objective>
Build the three pure TypeScript services that back background face processing and gap-based grouping. No React imports anywhere — these are testable pure modules per CLAUDE.md SOLID rules.

Purpose: Move face detection/embedding off the capture path into a durable queue, and provide session clustering that the Moments screen and WorkManager consume.
Output: FaceProcessingQueue, GapClusterService, RegroupService with Jest unit tests for the pure logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@CLAUDE.md
@src/services/cameraCaptureFlow.ts
@src/services/MomentCaptureService.ts
@src/db/queries/moments.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GapClusterService — pure gap clustering with tests</name>
  <read_first>
    - src/services/cameraCaptureFlow.ts (pure-service style, no React)
    - src/services/__tests__ (existing Jest test layout/conventions)
    - jest.config.js (test match patterns)
    - CLAUDE.md (pure functions in services; testable without RN preset)
  </read_first>
  <behavior>
    - Empty input → returns []
    - Single moment → one group containing that moment, startedAt=endedAt=occurredAt
    - Moments within GAP_THRESHOLD_MS of each other → one group
    - A gap strictly greater than GAP_THRESHOLD_MS → splits into a new group
    - Input order independent: unsorted input is sorted ascending by occurredAt first
    - Each group reports startedAt (min) and endedAt (max) of its members
  </behavior>
  <action>
    Create src/services/GapClusterService.ts exporting `GAP_THRESHOLD_MS = 60 * 60 * 1000` and a pure function `cluster(moments: { id: string; occurredAt: number; latitude?: number | null; longitude?: number | null }[]): { groupId: string; momentIds: string[]; startedAt: number; endedAt: number }[]`. Sort by occurredAt ascending, then walk the list starting a new session whenever the gap from the previous moment exceeds GAP_THRESHOLD_MS. Generate groupId with a local id helper (same Math.random+Date.now pattern used in MomentCaptureService) — but tests must NOT assert on groupId value, only structure. No React, no DB.

    Create src/services/__tests__/GapClusterService.test.ts covering every case in <behavior>.
  </action>
  <verify>
    <automated>npx jest src/services/__tests__/GapClusterService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - GAP_THRESHOLD_MS exported equals 3600000
    - cluster is pure (no imports from react, react-native, or DB)
    - All behavior cases pass
  </acceptance_criteria>
  <done>GapClusterService passes its full test suite.</done>
</task>

<task type="auto">
  <name>Task 2: FaceProcessingQueue — enqueue + idempotent drain</name>
  <read_first>
    - src/db/queries/processing_queue.ts (from 03-01 — claim/mark functions)
    - src/db/queries/moment_faces.ts (from 03-01 — insert face)
    - src/services/cameraCaptureFlow.ts (runDetect, runEmbedAndMatch signatures + HokedexML usage)
    - src/types/ml.ts (BoundingBox, HokedexMLNative)
    - CLAUDE.md (no React in services; composition not coupling — pass db + HokedexML as params)
  </read_first>
  <action>
    Create src/services/FaceProcessingQueue.ts as a pure module (no React). Export:

    `enqueue(db, momentId, photoUri)` — inserts a pending row via the EnqueueJob query (generate id locally, created_at=Date.now()).

    `drain(db, modules, limit = 5)` where modules is `{ HokedexML }` — loop up to `limit`: ClaimNextPending(1); if none, stop. Mark job 'processing'. Call runDetect({HokedexML}, photoUri, 'people'). For each detected face, InsertMomentFace with bbox + status 'detected'. If an embedding/match is available, call runEmbedAndMatch and on a 'match' result UpdateFaceEntry(entry_id, status='matched'), else leave status 'detected'. On success MarkJobDone(processed_at=Date.now()). On thrown error MarkJobFailed and continue to next job (do not abort the whole drain). Idempotency: ClaimNextPending only selects status='pending', so already-done jobs are never reprocessed.

    Reuse runDetect/runEmbedAndMatch from cameraCaptureFlow.ts — do not reimplement ML calls. Do not import MomentCaptureService here (avoid cycle; MomentCaptureService imports enqueue, not the reverse).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/services/FaceProcessingQueue.ts','utf8');['export','enqueue','drain','runDetect'].forEach(t=>{if(!s.includes(t))throw new Error('missing '+t)});if(/from ['\"]react/.test(s))throw new Error('react import in service');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - enqueue and drain exported; no react/react-native imports
    - drain claims only pending jobs, inserts moment_faces, marks done/failed per job
    - ML work delegated to cameraCaptureFlow functions
  </acceptance_criteria>
  <done>FaceProcessingQueue compiles, drain is idempotent and per-job fault-tolerant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RegroupService — merge-only regroup with concurrency state machine</name>
  <read_first>
    - src/services/GapClusterService.ts (cluster output shape — from Task 1)
    - src/db/queries/moment_groups.ts (from 03-01 — insert/find/update/members)
    - src/db/queries/moments.ts (reading moments with timestamps + coords)
    - CLAUDE.md (state machine logic belongs in service, never inline)
  </read_first>
  <behavior>
    - When idle and regroup() is called, state becomes 'running' then returns to 'idle' on completion
    - When regroup() is called while 'running', state becomes 'queued' and a second run executes after the first completes
    - Two overlapping calls result in exactly two total runs, never concurrent
    - Merge semantics: an existing group matching a cluster's start range is expanded (members added via INSERT OR IGNORE, ended_at updated) — never deleted; a cluster with no matching group inserts a new group + members
  </behavior>
  <action>
    Create src/services/RegroupService.ts exporting `regroup(db): Promise<void>` and a `regroupStatus` accessor/callback ('idle' | 'running' | 'queued') for UI. Maintain module-level state. On regroup(): if 'running', set 'queued' and return; set 'running'; fetch all moments (id, occurred_at, latitude, longitude); call GapClusterService.cluster(); for each cluster, FindGroupByStartRange(startedAt ± a small window) — if found, InsertGroupMember (OR IGNORE) for each momentId and UpdateGroupBounds(endedAt); else InsertGroup + InsertGroupMember for each. On finish, if state was bumped to 'queued', reset to 'running' and re-run once; otherwise set 'idle'. Expose a way to subscribe to status changes (callback registration) for the spinner in 03-04.

    Create src/services/__tests__/RegroupService.test.ts: mock the DB query layer (jest.mock the query modules) and assert the state-machine cases in <behavior> and that no delete query is ever called.
  </action>
  <verify>
    <automated>npx jest src/services/__tests__/RegroupService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - regroup serialises overlapping calls (idle/running/queued); never runs concurrently
    - merge-only: tests confirm no group deletion path
    - regroupStatus subscribable for UI spinner
  </acceptance_criteria>
  <done>RegroupService passes its test suite including the queued re-run case.</done>
</task>

</tasks>

<verification>
- `npx jest src/services/__tests__/GapClusterService.test.ts src/services/__tests__/RegroupService.test.ts` passes
- No `from 'react'` / `from 'react-native'` import in any of the three services
- `npx tsc --noEmit` introduces no new errors in touched files
</verification>

<success_criteria>
The three services exist as pure modules with passing unit tests; drain processes the queue idempotently and regroup merges sessions without ever wiping existing groups.
</success_criteria>

<output>
Create `.planning/phases/03-camera-capture-path/03-02-SUMMARY.md` when done.
</output>
