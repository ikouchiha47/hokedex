---
phase: 03-camera-capture-path
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/sql/migrations/011_moments_metadata.sql
  - src/db/sql/loader.ts
  - src/db/migrations/runner.ts
  - src/db/sql/queries/moments.sql
  - src/db/queries/moments.ts
  - src/services/captureMetadataService.ts
  - src/services/captureWeather.ts
  - src/services/MomentCaptureService.ts
autonomous: true
requirements: [R-CAM-02, R-DB-02]

must_haves:
  truths:
    - "moments table has source, latitude, longitude, place_name, weather_temp, weather_condition, type columns after migration 011"
    - "captureMetadataService returns GPS coords, place_name, and weather for a capture without blocking on failure"
    - "MomentCaptureService CaptureInput carries the full metadata set"
  artifacts:
    - path: "src/db/sql/migrations/011_moments_metadata.sql"
      provides: "ALTER TABLE adding 7 metadata columns to moments"
      contains: "ALTER TABLE moments ADD COLUMN"
    - path: "src/services/captureMetadataService.ts"
      provides: "Pure async GPS + place_name + weather resolver returning CaptureMetadata"
      contains: "getLocation"
    - path: "src/services/captureWeather.ts"
      provides: "Capture-time Open-Meteo wrapper returning temp + condition"
      contains: "wmoToCondition"
  key_links:
    - from: "src/services/captureMetadataService.ts"
      to: "HokedexGeocoder.getLocation / getCityName"
      via: "NativeModules"
      pattern: "HokedexGeocoder"
    - from: "src/services/captureWeather.ts"
      to: "Open-Meteo forecast API"
      via: "fetch + wmoToCondition"
      pattern: "open-meteo|wmoToCondition"
---

<objective>
Add the GPS + weather auto-attach data layer for capture. Create migration 011 adding the missing `moments` metadata columns (per R-DB-02), build a pure `captureMetadataService` that resolves GPS coords + place_name + weather at capture time (per R-CAM-02), and extend `MomentCaptureService.CaptureInput` to carry the full metadata set. GeocoderModule is already implemented and already registered in HokedexMLPackage.kt — no native work needed.

Purpose: 03-03 wires this metadata into the atomic moment write. This plan provides the schema and the metadata resolver so capture can attach lat/lon/place/weather to every moment.
Output: Migration 011, captureWeather.ts, captureMetadataService.ts, extended insertMoment + CaptureInput.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/CONVENTIONS.md
@.planning/phases/03-camera-capture-path/03-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 011 — add metadata columns to moments + extend insertMoment</name>
  <read_first>
    - src/db/sql/migrations/010_app_settings.sql (slot 010 taken; 011 is next)
    - src/db/sql/migrations/007_moments.sql (current moments schema — id, note, occurred_at, place_id, status, created_at)
    - src/db/sql/loader.ts (registry pattern: import migrationNNN + add to SQL object)
    - src/db/migrations/runner.ts (MIGRATIONS array — add { version: 11, sql: SQL.migration011 })
    - src/db/sql/queries/moments.sql (InsertMoment query)
    - src/db/queries/moments.ts (insertMoment signature + Moment type)
  </read_first>
  <action>
    Create src/db/sql/migrations/011_moments_metadata.sql with seven non-destructive statements (per R-DB-02): `ALTER TABLE moments ADD COLUMN source TEXT;`, `latitude REAL;`, `longitude REAL;`, `place_name TEXT;`, `weather_temp INTEGER;`, `weather_condition TEXT;`, `type TEXT;` — one ALTER per statement, all nullable, no defaults. Register in loader.ts: add `import migration011 from './migrations/011_moments_metadata.sql';` and add `migration011,` to the SQL object. Register in runner.ts: add `{ version: 11, sql: SQL.migration011 },` to the MIGRATIONS array. Update src/db/sql/queries/moments.sql InsertMoment to insert the new columns: `INSERT INTO moments (id, note, occurred_at, place_id, source, latitude, longitude, place_name, weather_temp, weather_condition, type, status, created_at) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?);` (status uses passed value or 'logged'). Update insertMoment in src/db/queries/moments.ts to accept a single params object: `{ note, occurredAt, placeId, source, latitude, longitude, placeName, weatherTemp, weatherCondition, type }` (all optional except note/occurredAt), generate id internally, pass all binds in order, return the generated id. Extend the Moment type with the new nullable fields. Keep executeSync for the write (it runs inside withTransaction). No raw SQL in the .ts file.
  </action>
  <verify>
    <automated>cd /Users/alexday/dev/hokedex && grep -c "ADD COLUMN" src/db/sql/migrations/011_moments_metadata.sql | grep -q "^7$" && grep -q "migration011" src/db/sql/loader.ts && grep -q "version: 11" src/db/migrations/runner.ts && grep -q "weather_condition" src/db/sql/queries/moments.sql && npx tsc --noEmit 2>&1 | grep -v node_modules | grep -c "moments.ts" | grep -q "^0$" && echo OK</automated>
  </verify>
  <done>Migration 011 adds 7 columns, registered in loader + runner, InsertMoment SQL and insertMoment() carry all metadata fields, tsc passes.</done>
  <acceptance_criteria>
    - 011_moments_metadata.sql contains exactly 7 ALTER TABLE ... ADD COLUMN statements.
    - loader.ts imports migration011 and includes it in SQL.
    - runner.ts MIGRATIONS array contains version 11.
    - insertMoment accepts the metadata params object and returns the generated id.
    - No raw SQL string literals added to moments.ts.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: captureWeather.ts — capture-time Open-Meteo wrapper</name>
  <read_first>
    - src/services/weather.ts (buildApiUrl + Open-Meteo pattern; do NOT call getWeather — it re-fetches location)
    - src/services/weatherScene.ts (wmoToCondition — line 20, reuse, do not reimplement)
    - src/services/weatherPrivacy.ts (roundCoordinates, getDeviceTimeZone)
    - src/types/weatherApi.ts (DEFAULT_WEATHER_API_CONFIG, WeatherApiRaw, parseResponse)
    - src/types/weather.ts (WeatherCondition enum values)
  </read_first>
  <action>
    Create src/services/captureWeather.ts exporting `type CaptureWeather = { temp: number; condition: WeatherCondition }` and `async function getWeatherForCapture(lat: number, lon: number): Promise<CaptureWeather | null>`. It takes already-resolved coordinates (do NOT call HokedexGeocoder.getLocation here — the caller already has coords, per RESEARCH pitfall), applies roundCoordinates before the request (coarse coords only leave device), builds the Open-Meteo URL the same way weather.ts does (reuse buildApiUrl logic or DEFAULT_WEATHER_API_CONFIG.url), fetches, parses via config.parseResponse into WeatherApiRaw, returns `{ temp: Math.round(raw.temperature_2m), condition: wmoToCondition(raw.weather_code) }`. On any fetch/network error, catch and return null (offline must not block capture). Import wmoToCondition from weatherScene.ts — do not reimplement WMO mapping.
  </action>
  <verify>
    <automated>cd /Users/alexday/dev/hokedex && grep -q "getWeatherForCapture" src/services/captureWeather.ts && grep -q "wmoToCondition" src/services/captureWeather.ts && grep -q "roundCoordinates" src/services/captureWeather.ts && ! grep -q "getLocation" src/services/captureWeather.ts && npx tsc --noEmit 2>&1 | grep -v node_modules | grep -c "captureWeather" | grep -q "^0$" && echo OK</automated>
  </verify>
  <done>getWeatherForCapture returns temp+condition for given coords, returns null on network failure, reuses wmoToCondition, does not re-fetch location.</done>
  <acceptance_criteria>
    - captureWeather.ts exports getWeatherForCapture(lat, lon).
    - wmoToCondition is imported from weatherScene.ts (not redefined).
    - roundCoordinates applied before the API call.
    - No call to HokedexGeocoder.getLocation in this file.
    - Returns null inside a catch on fetch error.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: captureMetadataService.ts + extend CaptureInput</name>
  <read_first>
    - src/services/weather.ts (HokedexGeocoder.getLocation / getCityName usage + ACCESS_FINE_LOCATION request pattern)
    - src/services/captureWeather.ts (getWeatherForCapture — created in Task 2)
    - src/services/MomentCaptureService.ts (CaptureInput type to extend)
    - android/app/src/main/java/com/hokedex/ml/HokedexMLPackage.kt (GeocoderModule already registered — confirm, no change)
  </read_first>
  <action>
    Create src/services/captureMetadataService.ts exporting `type CaptureMetadata = { latitude: number | null; longitude: number | null; placeName: string | null; weatherTemp: number | null; weatherCondition: string | null }` and `async function resolveCaptureMetadata(): Promise<CaptureMetadata>`. Pure async, no DB and no UI. Request ACCESS_FINE_LOCATION (reuse the PermissionsAndroid pattern from weather.ts). Call `NativeModules.HokedexGeocoder.getLocation()` for coords; on NO_LOCATION rejection catch and return all-null metadata (capture must continue, RESEARCH pitfall). If coords obtained, run `getCityName(lat, lon)` and `getWeatherForCapture(lat, lon)` in parallel via Promise.all, each wrapped so a single failure yields null for that field (place_name null if Geocoder throws offline; weather null if network down). Return the assembled CaptureMetadata. Do not round coords here — getWeatherForCapture rounds internally before sending; store full coords on the moment.

    Then extend CaptureInput in src/services/MomentCaptureService.ts to add: `source?: 'camera' | 'voice' | 'gallery'`, `latitude?: number | null`, `longitude?: number | null`, `placeName?: string | null`, `weatherTemp?: number | null`, `weatherCondition?: string | null`, `type?: string | null`. Do NOT implement capture() in this plan — that is 03-03. Only the type changes here.
  </action>
  <verify>
    <automated>cd /Users/alexday/dev/hokedex && grep -q "resolveCaptureMetadata" src/services/captureMetadataService.ts && grep -q "HokedexGeocoder" src/services/captureMetadataService.ts && grep -q "weatherTemp" src/services/MomentCaptureService.ts && grep -q "getWeatherForCapture" src/services/captureMetadataService.ts && npx tsc --noEmit 2>&1 | grep -v node_modules | grep -cE "captureMetadataService|MomentCaptureService" | grep -q "^0$" && echo OK</automated>
  </verify>
  <done>resolveCaptureMetadata returns full metadata (or nulls on failure) without throwing; CaptureInput carries the metadata fields; tsc passes.</done>
  <acceptance_criteria>
    - captureMetadataService.ts exports resolveCaptureMetadata() returning CaptureMetadata.
    - NO_LOCATION / Geocoder / weather failures each degrade to null, never throw out of the function.
    - CaptureInput includes source, latitude, longitude, placeName, weatherTemp, weatherCondition, type.
    - capture() body is NOT implemented in this plan (still the stub).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device GPS → app | Location permission gates coordinate access |
| app → Open-Meteo | Outbound network request with coarse coordinates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information disclosure | coordinates sent to Open-Meteo | mitigate | roundCoordinates() coarsens lat/lon before fetch (R-NF-01) |
| T-03-04 | Denial of service | offline at capture | mitigate | GPS/geocode/weather failures degrade to null; capture never blocks |
| T-03-05 | Tampering | migration applied to live DB | accept | non-destructive ALTER only; WAL/schema_migrations tracked by runner |
</threat_model>

<verification>
- Migration 011 applies cleanly; `sqlite3 hokedex.db ".schema moments"` shows the 7 new columns.
- resolveCaptureMetadata returns coords + place + weather on device with permissions granted.
- Offline / permission-denied path returns nulls without throwing.
</verification>

<success_criteria>
- R-DB-02: moments has source, latitude, longitude, place_name, weather_temp, weather_condition, type.
- R-CAM-02 (data half): GPS + weather resolvable at capture time via a single service call.
</success_criteria>

<output>
Create `.planning/phases/03-camera-capture-path/03-02-SUMMARY.md` when done.
</output>
