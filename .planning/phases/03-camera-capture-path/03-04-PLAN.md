---
phase: 03-camera-capture-path
plan: 04
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified:
  - src/screens/MomentsScreen.tsx
  - src/components/moments/MomentGroupCarousel.tsx
  - src/components/moments/MomentLocationCards.tsx
autonomous: false
requirements: [R-CAM-04, R-CAM-07, R-CAM-08]
must_haves:
  truths:
    - "Moments screen renders gap-clustered groups read from moment_groups + members"
    - "Layout toggle switches between carousel (Layout A) and location cards (Layout B); preference persists in app_settings"
    - "A re-group button calls RegroupService.regroup and shows a spinner while running, respecting the concurrency state"
    - "Tapping a face chip navigates to entry detail or new-entry creation (face annotation entry point)"
  artifacts:
    - path: "src/screens/MomentsScreen.tsx"
      provides: "grouped timeline with layout toggle + regroup button"
      min_lines: 60
    - path: "src/components/moments/MomentGroupCarousel.tsx"
      provides: "Layout A horizontal photo carousel per group"
    - path: "src/components/moments/MomentLocationCards.tsx"
      provides: "Layout B location-cluster cards per group"
  key_links:
    - from: "src/screens/MomentsScreen.tsx"
      to: "src/services/RegroupService.ts"
      via: "regroup() on button press"
      pattern: "regroup"
    - from: "src/screens/MomentsScreen.tsx"
      to: "src/db/queries/moment_groups.ts"
      via: "ListGroups + members read"
      pattern: "moment_groups|ListGroups"
    - from: "src/screens/MomentsScreen.tsx"
      to: "src/db/queries/app_settings.ts"
      via: "GetSetting/UpsertSetting for moments_layout"
      pattern: "moments_layout"
---

<objective>
Build the Moments screen: a gap-clustered timeline of moment groups with two layout modes, a persisted layout toggle, a re-group action, and the face-annotation entry point. This is where face annotation happens — not during capture.

Purpose: Surface the grouped capture history and let the user organise and annotate moments after the fact.
Output: Full MomentsScreen replacing the stub, plus the two layout components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@CLAUDE.md
@src/screens/MomentsScreen.tsx
@src/navigation/TabNavigator.tsx
@src/navigation/types.ts
@src/db/queries/app_settings.ts
@src/services/RegroupService.ts
@src/services/MomentCaptureService.ts
@src/AppContext.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Layout components — carousel (A) and location cards (B)</name>
  <read_first>
    - src/screens/CollectionListScreen.tsx (existing list/card styling + Win10-mobile design language)
    - src/db/queries/moments.ts (moment row shape: occurred_at, latitude, longitude, place_name)
    - src/db/queries/moment_faces.ts (from 03-01 — ListFacesByMoment for face chips)
    - CLAUDE.md (no decision logic inline in components; constants at top; lucide icons only, no emoji)
  </read_first>
  <action>
    Create src/components/moments/MomentGroupCarousel.tsx (Layout A) — pure presentational: receives a group ({label, startedAt, endedAt, momentIds}) and its moments (with photo thumbnails + face chips), renders a horizontal ScrollView of photo thumbnails. Header shows date range, photo count, place_name if any.

    Create src/components/moments/MomentLocationCards.tsx (Layout B) — pure presentational: receives the same group + moments, sub-clusters its moments by place_name (or rounded lat/lon when place_name absent) and renders one card per location cluster with location name + photo count + thumbnails.

    Both accept an `onFaceChipPress(face)` callback prop (face carries entry_id nullable + moment_id). Neither does data fetching — props only. Use lucide-react-native icons from the icons barrel, no emoji.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');['MomentGroupCarousel','MomentLocationCards'].forEach(c=>{const s=fs.readFileSync('src/components/moments/'+c+'.tsx','utf8');if(!s.includes('onFaceChipPress'))throw new Error(c+' missing onFaceChipPress');if(/from ['\"]\.\.\/\.\.\/db/.test(s))throw new Error(c+' must not fetch data')});console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Both components are presentational (props only, no DB import)
    - Carousel renders horizontal photos per group; location cards sub-cluster by location
    - Both expose onFaceChipPress; lucide icons only, no emoji
  </acceptance_criteria>
  <done>Both layout components render from props and surface face chip taps.</done>
</task>

<task type="auto">
  <name>Task 2: MomentsScreen — grouped data, layout toggle, regroup button, face annotation</name>
  <read_first>
    - src/screens/MomentsScreen.tsx (current stub)
    - src/db/queries/moment_groups.ts (ListGroups, ListMembersByGroup / ListAllGroupMembers)
    - src/db/queries/moments.ts (read moment rows for member ids)
    - src/db/queries/app_settings.ts (GetSetting/UpsertSetting wrappers)
    - src/services/RegroupService.ts (regroup + regroupStatus subscription)
    - src/services/MomentCaptureService.ts (addPeopleToMoment for annotation create path)
    - src/navigation/types.ts (EntryDetail + NewEntry route params for face annotation nav)
    - src/AppContext.tsx (useApp() db handle)
  </read_first>
  <action>
    Replace the MomentsScreen stub. On focus, load groups via ListGroups + members, hydrate each group's moments (with face chips via ListFacesByMoment). Read layout preference from app_settings key 'moments_layout' (values 'carousel' | 'location', default 'carousel'); render MomentGroupCarousel or MomentLocationCards accordingly. A toggle control switches layout and persists via UpsertSetting('moments_layout', ...).

    Add a re-group button at the top: on press call RegroupService.regroup(db); subscribe to regroupStatus to show a spinner while 'running' or 'queued' and disable the button; on completion reload groups. Do not implement concurrency in the screen — RegroupService owns it; the screen only reflects status.

    Face annotation entry point (R-CAM-04 / R-CAM-07 / R-CAM-08): onFaceChipPress — if the face has an entry_id, navigate to EntryDetail({entryId}); if entry_id is null (anonymous), navigate to NewEntry (or a name prompt) to create/link a person, then on return persist the link via MomentCaptureService. Keep this branching logic in a small handler in the screen, not inside the presentational components.

    Keep MomentsScreen wired in TabNavigator (already present) — no nav changes needed beyond using existing routes.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/screens/MomentsScreen.tsx','utf8');['moments_layout','regroup','ListGroups','onFaceChipPress','EntryDetail'].forEach(t=>{if(!s.includes(t))throw new Error('missing '+t)});console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Screen reads moment_groups + members and renders the selected layout
    - Layout toggle persists to app_settings; default carousel
    - Re-group button calls RegroupService and reflects running/queued via spinner
    - Face chip tap navigates to EntryDetail (known) or NewEntry (anonymous) and persists link via MomentCaptureService
  </acceptance_criteria>
  <done>MomentsScreen renders grouped timeline with working toggle, regroup, and face annotation entry point.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Moments screen: gap-clustered groups, Layout A/B toggle (persisted), re-group button with spinner, face-chip annotation entry point. Capture path (03-03) now enqueues face jobs instead of blocking; background worker drains them.</what-built>
  <how-to-verify>
    1. Build + install per CLAUDE.md release flow (cd android && ./gradlew assembleRelease --no-build-cache && adb install -r ... && force-stop + start).
    2. Capture 2-3 photos on the Camera tab — confirm the shutter returns immediately with no face picker modal.
    3. Open the Moments tab — confirm grouped cards appear (moments within 1h grouped together).
    4. Tap the layout toggle — confirm it switches carousel <-> location cards and the choice survives a tab switch away and back.
    5. Tap Re-group — confirm a spinner shows and groups refresh; tap it twice quickly and confirm no crash (queued run).
    6. Tap a face chip — confirm it navigates to a person/entry screen.
    7. Pull the DB (force-stop first) and confirm processing_queue + moment_faces rows exist.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` introduces no new errors
- Layout preference round-trips through app_settings
- Re-group reflects RegroupService status without the screen owning concurrency
</verification>

<success_criteria>
The Moments screen shows gap-clustered groups in either layout (persisted), lets the user re-group with a spinner, and provides the face annotation entry point that links anonymous faces to people.
</success_criteria>

<output>
Create `.planning/phases/03-camera-capture-path/03-04-SUMMARY.md` when done.
</output>
