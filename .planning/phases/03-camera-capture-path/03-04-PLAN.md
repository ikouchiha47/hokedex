---
phase: 03-camera-capture-path
plan: 04
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - src/screens/CameraScreen.tsx
autonomous: true
requirements: [R-CAM-10]
gap_closure: true

must_haves:
  truths:
    - "Tapping capture in voice mode triggers GPS + weather auto-attach and saves a voice moment (source='voice', type='voice') with no people linked"
    - "Voice mode capture completes with a ToastAndroid confirmation even when GPS or weather is unavailable"
  artifacts:
    - path: "src/screens/CameraScreen.tsx"
      provides: "Voice mode capture branch inside handleCapture"
      contains: "captureMode === 'voice'"
  key_links:
    - from: "src/screens/CameraScreen.tsx"
      to: "MomentCaptureService.capture"
      via: "voice branch in handleCapture"
      pattern: "source.*voice"
---

<objective>
Close the R-CAM-10 gap: voice mode currently switches the UI label but tapping capture in voice mode has no implementation. This plan adds the voice-mode capture branch to CameraScreen.handleCapture so that tapping capture while in voice mode resolves GPS + weather metadata and saves a moment with source='voice', type='voice', no people linked, and a toast confirmation. Full STT (Phase 5) is still deferred — the seam is the empty note field.

Purpose: R-CAM-10 requires the metadata-attach flow runs for voice mode, not just camera mode. Without this branch, voice capture is a dead tap.
Output: CameraScreen.tsx with a voice-mode capture branch that calls resolveCaptureMetadata + MomentCaptureService.capture with source/type='voice'.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/CONVENTIONS.md
@.planning/phases/03-camera-capture-path/03-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add voice-mode capture branch to handleCapture in CameraScreen</name>
  <read_first>
    - src/screens/CameraScreen.tsx (post-03-03 state — handleCapture, captureMode state, resolveCaptureMetadata call, MomentCaptureService.capture call)
    - src/services/captureMetadataService.ts (resolveCaptureMetadata signature)
    - src/services/MomentCaptureService.ts (CaptureInput — source field values: 'camera' | 'voice' | 'gallery')
  </read_first>
  <action>
    In CameraScreen.tsx, inside handleCapture, add a branch at the very top: `if (captureMode === 'voice') { ... return; }`. Inside this branch: (1) call `await resolveCaptureMetadata()` to get GPS + weather (same as camera path — per R-CAM-10 "same metadata-attach flow"); (2) call `await MomentCaptureService.capture({ note: null, occurredAt: Date.now(), entryIds: [], newPeople: [], source: 'voice', type: 'voice', latitude: metadata.latitude, longitude: metadata.longitude, placeName: metadata.placeName, weatherTemp: metadata.weatherTemp, weatherCondition: metadata.weatherCondition })` — no photo URI, no face detection, no FacePickerModal; (3) show `ToastAndroid.show('Voice moment saved', ToastAndroid.SHORT)` on success or an Alert on capture error. Do NOT launch any audio recording API — that is Phase 5 (STT). The `note` stays null as the seam for future transcription. No other code changes outside the voice branch. Keep the existing camera/video branch untouched.
  </action>
  <verify>
    <automated>cd /Users/alexday/dev/hokedex && grep -q "captureMode === 'voice'" src/screens/CameraScreen.tsx && grep -q "source.*voice" src/screens/CameraScreen.tsx && npx tsc --noEmit 2>&1 | grep -v node_modules | grep -c "CameraScreen" | grep -q "^0$" && echo OK</automated>
  </verify>
  <done>Tapping capture while in voice mode saves a moment with source='voice' and metadata attached; no audio recording is attempted; tsc passes with no errors in CameraScreen.tsx.</done>
  <acceptance_criteria>
    - CameraScreen.tsx contains a `captureMode === 'voice'` branch at the top of handleCapture that returns early after saving.
    - The voice branch calls resolveCaptureMetadata() and passes latitude, longitude, placeName, weatherTemp, weatherCondition to capture().
    - CaptureInput has source: 'voice' and type: 'voice'.
    - No audio recording API (MediaRecorder, AudioRecord, etc.) is invoked.
    - note: null in the voice capture call (Phase 5 STT seam).
    - ToastAndroid fires on successful save.
    - No regression in the camera capture branch (existing grep assertions from 03-03 still pass).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → moments DB | Voice moment write still funnels through MomentCaptureService (R-CONV-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Information disclosure | GPS on voice moment | mitigate | resolveCaptureMetadata already applies roundCoordinates before Open-Meteo; stored coords are raw but on-device only |
| T-03-10 | Denial of service | voice capture without GPS/network | mitigate | resolveCaptureMetadata already degrades to nulls; capture proceeds with null metadata fields |
</threat_model>

<verification>
- Switch to voice mode, tap capture → ToastAndroid "Voice moment saved" appears.
- sqlite3 check: `SELECT source, type, note, latitude FROM moments ORDER BY created_at DESC LIMIT 1` shows source='voice', type='voice', note=NULL, latitude populated (or NULL if GPS unavailable).
- Camera mode still works end-to-end after this change.
</verification>

<success_criteria>
- R-CAM-10: Voice capture mode attaches GPS + weather and saves a moment. Mode switch is functional. Full STT deferred to Phase 5 by design (note=null seam).
</success_criteria>

<output>
Create `.planning/phases/03-camera-capture-path/03-04-SUMMARY.md` when done.
</output>
